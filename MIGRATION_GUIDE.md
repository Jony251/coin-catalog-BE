# Руководство по переносу функционала на Backend

## Что нужно и можно перенести на Backend

### ✅ 1. Аутентификация и авторизация (КРИТИЧНО)

**Текущее состояние:**
- Аутентификация через Firebase Auth выполняется на клиенте
- Токены хранятся локально

**Что переносим:**
- ✅ Верификация токенов на сервере
- ✅ Middleware для защиты API endpoints
- ✅ Управление сессиями пользователей
- ✅ Регистрация и вход через API

**Файлы для изменения:**
- `coin_catalog/services/FirebaseService.js` - оставить только клиентскую часть
- Добавить вызовы к backend API вместо прямых Firebase запросов

---

### ✅ 2. Управление коллекцией пользователя (ВЫСОКИЙ ПРИОРИТЕТ)

**Текущее состояние:**
- `UserCollectionService.js` работает с локальной БД (SQLite/localStorage)
- Синхронизация с Firebase выполняется напрямую с клиента

**Что переносим:**
- ✅ CRUD операции с коллекцией через API
- ✅ Синхронизация данных между устройствами
- ✅ Валидация данных на сервере
- ✅ Управление конфликтами при синхронизации

**Преимущества:**
- Единый источник правды (single source of truth)
- Защита от манипуляций на клиенте
- Централизованная бизнес-логика
- Проще добавлять новые функции

**Изменения в клиенте:**
```javascript
// Вместо прямого обращения к Firebase
await firebaseService.addCoinToCollection(userId, coin);

// Будет обращение к API
await fetch(`${API_URL}/api/v1/collection`, {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify(coin)
});
```

---

### ✅ 3. Список желаний (ВЫСОКИЙ ПРИОРИТЕТ)

**Что переносим:**
- ✅ Управление wishlist через API
- ✅ Синхронизация между устройствами
- ✅ Перемещение монет из wishlist в коллекцию

---

### ✅ 4. Синхронизация данных (КРИТИЧНО)

**Текущее состояние:**
- Каждый клиент синхронизируется с Firebase независимо
- Возможны конфликты при одновременной работе с разных устройств

**Что переносим:**
- ✅ Централизованная логика синхронизации
- ✅ Разрешение конфликтов (conflict resolution)
- ✅ Batch операции для оптимизации
- ✅ Отслеживание статуса синхронизации

---

### ⚠️ 5. Каталог монет (СРЕДНИЙ ПРИОРИТЕТ)

**Текущее состояние:**
- `DatabaseService.js` загружает статические данные каталога
- Данные хранятся в `data/catalog.js`, `data/ussr_catalog.js`, etc.

**Варианты:**

#### Вариант A: Оставить на клиенте (рекомендуется для начала)
- ✅ Каталог статичен и редко меняется
- ✅ Не требует аутентификации
- ✅ Быстрая работа без сетевых запросов
- ❌ Нужно обновлять приложение для новых монет

#### Вариант B: Перенести на backend (для масштабирования)
- ✅ Централизованное управление каталогом
- ✅ Обновления без релиза приложения
- ✅ Возможность добавления пользовательских монет
- ✅ API для поиска и фильтрации
- ❌ Требует сетевых запросов
- ❌ Нужна стратегия кэширования

**Рекомендация:** Начать с варианта A, позже мигрировать на B

---

### ❌ 6. Локальное хранилище (ОСТАВИТЬ НА КЛИЕНТЕ)

**Что НЕ переносим:**
- ❌ SQLite/localStorage для offline работы
- ❌ Кэширование данных каталога
- ❌ Локальные изображения пользователя

**Причина:** Необходимо для работы без интернета

---

## План миграции

### Этап 1: Подготовка (текущий)
- [x] Создать структуру backend
- [x] Настроить Firebase Admin SDK
- [x] Создать API endpoints
- [ ] Настроить .env с credentials

### Этап 2: Аутентификация
- [ ] Добавить API клиент в frontend
- [ ] Заменить прямые вызовы Firebase на API вызовы
- [ ] Добавить хранение токенов
- [ ] Тестирование

### Этап 3: Коллекция пользователя
- [ ] Обновить `UserCollectionService.js` для работы с API
- [ ] Добавить offline-first стратегию
- [ ] Реализовать queue для синхронизации
- [ ] Тестирование

### Этап 4: Синхронизация
- [ ] Реализовать conflict resolution
- [ ] Добавить batch операции
- [ ] Оптимизировать сетевые запросы
- [ ] Тестирование

### Этап 5: Опционально - Каталог
- [ ] API для получения каталога
- [ ] Стратегия кэширования
- [ ] Инкрементальные обновления

---

## Изменения в клиентском коде

### 1. Создать API клиент

```javascript
// services/ApiClient.js
class ApiClient {
  constructor(baseURL) {
    this.baseURL = baseURL;
  }

  async request(endpoint, options = {}) {
    const token = await this.getToken();
    
    const response = await fetch(`${this.baseURL}${endpoint}`, {
      ...options,
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${token}`,
        ...options.headers,
      },
    });

    if (!response.ok) {
      throw new Error(`API Error: ${response.statusText}`);
    }

    return response.json();
  }

  async getToken() {
    // Получить токен из Firebase Auth
    const user = auth.currentUser;
    return user ? await user.getIdToken() : null;
  }
}

export const apiClient = new ApiClient('http://localhost:3000');
```

### 2. Обновить UserCollectionService

```javascript
// services/UserCollectionService.js
async addCoin(catalogCoinId, data = {}) {
  // Сначала сохраняем локально
  const userCoin = await this._addCoinLocally(catalogCoinId, data);
  
  // Затем синхронизируем с сервером
  try {
    await apiClient.request('/api/v1/collection', {
      method: 'POST',
      body: JSON.stringify({
        catalogCoinId,
        ...data,
      }),
    });
    
    userCoin.markAsSynced();
  } catch (error) {
    console.error('Sync error:', error);
    // Монета сохранена локально, синхронизируем позже
  }
  
  return userCoin;
}
```

---

## Конфигурация

### Backend (.env)
```env
PORT=3000
NODE_ENV=development
FIREBASE_PROJECT_ID=coin-catalog-82641
FIREBASE_CLIENT_EMAIL=your-email@project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
ALLOWED_ORIGINS=http://localhost:8081,http://localhost:19006
```

### Frontend (config)
```javascript
// config/api.js
export const API_CONFIG = {
  baseURL: __DEV__ 
    ? 'http://localhost:3000' 
    : 'https://your-production-api.com',
  timeout: 10000,
};
```

---

## Безопасность

### ✅ Что реализовано:
- JWT верификация через Firebase Admin SDK
- CORS защита
- Helmet для HTTP headers
- Валидация входных данных
- Error handling

### 🔜 Что добавить:
- Rate limiting
- Request logging
- API versioning
- Мониторинг и алерты

---

## Тестирование API

### Примеры запросов:

```bash
# Регистрация
curl -X POST http://localhost:3000/api/v1/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"test123","displayName":"Test User"}'

# Получить коллекцию
curl -X GET http://localhost:3000/api/v1/collection \
  -H "Authorization: Bearer YOUR_TOKEN"

# Добавить монету
curl -X POST http://localhost:3000/api/v1/collection \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"catalogCoinId":"coin_123","condition":"good","notes":"Отличное состояние"}'
```

---

## Следующие шаги

1. **Настроить Firebase Admin SDK credentials**
   - Создать service account в Firebase Console
   - Скачать JSON ключ
   - Добавить в .env

2. **Установить зависимости и запустить backend**
   ```bash
   cd back_end
   npm install
   npm run dev
   ```

3. **Создать API клиент во frontend**
   - Создать `services/ApiClient.js`
   - Настроить конфигурацию

4. **Постепенно мигрировать функции**
   - Начать с аутентификации
   - Затем коллекция
   - Затем синхронизация

5. **Тестировать на каждом этапе**
   - Unit тесты
   - Integration тесты
   - E2E тесты
